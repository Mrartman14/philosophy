// src/i18n/messages/en/preferences.ts
// Зеркало ru/preferences.ts.
const preferences = {
  // Action phrase for "You don't have permission for {action}."
  pushSendAction: "sending push notifications",
};

export default preferences;
