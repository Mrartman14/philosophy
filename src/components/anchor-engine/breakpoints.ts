// ЕДИНАЯ константа wide-порога движка маргиналий. Колонка карточек
// (margin-notes-column), выноски-коннекторы (connector-layer) и хук useWide
// (use-wide.ts, гейт слайсов inline-vs-rail) ОБЯЗАНЫ включаться синхронно: линии
// осмысленны только когда карточки спозиционированы абсолютно в wide-режиме, а
// слайс не должен схлопывать inline-фолбэк раньше/позже, чем rail раскрыл колонку.
// ВАЖНО: значение `80rem` (НЕ `80em`) — сознательное foundation-решение для
// media-гейта rail; @container-порог marginalia-layout (`80em`) — отдельная ось,
// синхронить их НЕ нужно.
export const WIDE = "(min-width: 80rem)";
